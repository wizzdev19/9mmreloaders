# Cloudflare Pages config - static export from Express app
# Build command: npm run build (runs scripts/build-pages.js)
# Output: dist/
pages_build_output_dir = "dist"
compatibility_date = "2024-09-12"
compatibility_flags = ["nodejs_compat"]

[vars]
NODE_ENV = "production"
REQUIRE_BUSINESS = "0"
